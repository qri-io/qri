package dataset_sql

import (
	"testing"
)

func TestFmt(t *testing.T) {
	// TODO - finish
	// str := "select blah, table_b.bar from table_a, table_b limit 1"
	// formatted, stmt, remap, err := Format(str)
	// if stmt == nil {
	// 	t.Errorf("statement shouldn't be nil")
	// }
	// if err != nil {
	// 	t.Errorf("unexpected error: %s", err.Error())
	// 	return
	// }

	// // expect := "select "

	// expect := "select blah from a, b limit 1"
	// if formatted != expect {
	// 	t.Errorf("formatting error: %s != %s", expect, formatted)
	// }
	// if len(remap) != 2 {
	// 	t.Errorf("remap length error: %d != %d", 2, len(remap))
	// }
}
